name: Security Suite

on:
  pull_request:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  security-events: write

jobs:
  codeql:
    uses: SEU_ORG/security-workflows/.github/workflows/reusable-codeql.yml@v1

  semgrep:
    uses: SEU_ORG/security-workflows/.github/workflows/reusable-semgrep.yml@v1
    with:
      enforce_on_pr: true

  gitleaks:
    uses: SEU_ORG/security-workflows/.github/workflows/reusable-gitleaks.yml@v1
    with:
      enforce_on_pr: true

  trivy:
    uses: SEU_ORG/security-workflows/.github/workflows/reusable-trivy.yml@v1
    with:
      image_name: ghcr.io/${{ github.repository_owner }}/minha-api
      enforce_on_pr: true
