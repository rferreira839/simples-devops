apiVersion: v1
kind: Service
metadata:
  name: {{ include "minha-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "minha-api.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
