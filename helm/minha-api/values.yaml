image:
  repository: ghcr.io/rferreira839/minha-api  # << usa a imagem local que você buildou
  tag: "main-492154b"
  pullPolicy: IfNotPresent

replicaCount: 2

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources: {}

env: []

ingress:
  enabled: true
  className: nginx
  host: minha-api.local
  path: /
